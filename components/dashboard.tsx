"use client";

import { useState } from "react";
import {
  AlertTriangle, Archive, BarChart3, Bell, Bot, CalendarDays, CheckCircle2, ChevronRight, ClipboardCheck,
  Database, Download, FileText, Gauge, Import, LayoutDashboard, Mail, Menu, MessageCircle, Search,
  Send, Settings, ShieldCheck, Sparkles, Target, Upload, Users
} from "lucide-react";
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { audits, customers, demoAccounts, recommendations, riskChart, tiersChart, trendChart } from "@/lib/demo-data";
import type { AVOAnalysis } from "@/lib/avo";
import type { Customer, Role } from "@/lib/types";
import { canOutreach, detectSegmentDecline, whatsappLink } from "@/lib/engines";

const nav=[
 ["Workspace",[["overview","Overview",LayoutDashboard],["alerts","Alert Centre",Bell],["customers","Customers",Users],["conversations","Conversations",MessageCircle],["imports","Data Imports",Import]]],
 ["AVO",[["avo","AVO",Bot],["recommendations","Recommendations",Sparkles],["actions","Retention Actions",ClipboardCheck]]],
 ["Marketing",[["marketing","Marketing Intelligence",Target],["campaign-studio","Campaign Studio",Send],["campaign-calendar","Campaign Calendar",CalendarDays],["analytics","Analytics",BarChart3]]],
 ["Control",[["governance","Data Governance",ShieldCheck],["audit","Audit Reports",Archive],["settings","Settings",Settings]]]
] as const;
const titles:Record<string,[string,string]>={overview:["Retention intelligence at a glance","Prioritise customer recovery and governed marketing action."],alerts:["Alert Centre","Evidence-linked churn, complaint and segment alerts."],customers:["Customers","Customer health, value, risk and opportunity in one view."],conversations:["Conversations","Review authorised conversations and analyse evidence with AVO."],imports:["Data Import Centre","Validate, map and confirm customer data and approved source documents."],avo:["Ask AVO","Grounded answers and draft actions for authorised staff."],recommendations:["AVO Recommendations","Evidence-backed next actions awaiting human judgment."],actions:["Retention Actions","Review, approve and execute governed customer outreach."],marketing:["Marketing Intelligence","Detect segment decline and turn common evidence into interventions."],"campaign-studio":["Campaign Studio","Create grounded, channel-ready campaigns with AVO."],"campaign-calendar":["Campaign Calendar","Review approved and simulated publishing schedules."],analytics:["Analytics","Customer recovery, action effectiveness and AVO governance."],governance:["Data Governance","Privacy-supporting controls, consent, lineage and retention."],audit:["Audit Reports","Immutable histories of access, analysis, approval and execution."],settings:["Settings","Organisation roles, scoring thresholds and integrations."]};
const money=(n:number)=>new Intl.NumberFormat("en-MY",{style:"currency",currency:"MYR",maximumFractionDigits:0}).format(n);
const badge=(value:string)=><span className={`badge ${value.toLowerCase().replaceAll(" ","-")}`}>{value}</span>;

export function Dashboard({initialPage}:{initialPage:string}){
 const [page,setPage]=useState(titles[initialPage]?initialPage:"overview");
 const [role,setRole]=useState<Role>("Sales Manager");
 const [toast,setToast]=useState("");
 const go=(p:string)=>{setPage(p);window.history.pushState({},"",`/${p}`);window.scrollTo({top:0,behavior:"smooth"})};
 const notify=(m:string)=>{setToast(m);setTimeout(()=>setToast(""),3200)};
 const [title,sub]=titles[page];
 return <div className="shell">
  <aside className="sidebar">
   <div className="brand"><span className="brandmark">A</span><span>CustomerPulse <small style={{color:"#69d2bd"}}>AI</small></span></div>
   {nav.map(([group,items])=><div key={group}><div className="nav-group">{group}</div>{items.map(([key,label,Icon])=><a key={key} href={`/${key}`} onClick={e=>{e.preventDefault();go(key)}} className={`nav-link ${page===key?"active":""}`}><Icon/>{label}</a>)}</div>)}
  </aside>
  <main className="main">
   <header className="topbar"><div className="top-actions"><button className="btn btn-outline mobile-menu"><Menu size={16}/></button><span className="demo-label"><Database size={12}/> Synthetic Demo Data</span></div><div className="top-actions"><button className="btn btn-outline" onClick={()=>go("avo")}><Bot size={14}/> Ask AVO</button><select aria-label="Demo role" className="input" value={role} onChange={e=>setRole(e.target.value as Role)}><option>Administrator</option><option>Sales Manager</option><option>Marketing Manager</option><option>Account Executive</option><option>Auditor</option></select><div className="avatar">FR</div></div></header>
   <div className="content"><div className="title-row"><div><div className="eyebrow">CustomerPulse AI / {page.replaceAll("-"," ")}</div><h1>{title}</h1><div className="subtle">{sub}</div></div><div className="top-actions"><span className="demo-label">AVO Demo Analysis available</span></div></div>
    <Page page={page} go={go} notify={notify} role={role}/>
   </div>
  </main>{toast&&<div className="toast"><CheckCircle2 size={14} style={{verticalAlign:"middle",marginRight:8}}/>{toast}</div>}
 </div>
}

function Page({page,go,notify,role}:{page:string;go:(p:string)=>void;notify:(m:string)=>void;role:Role}){
 switch(page){case"overview":return <Overview go={go}/>;case"alerts":return <Alerts go={go} notify={notify}/>;case"customers":return <Customers notify={notify}/>;case"conversations":return <Conversations notify={notify}/>;case"imports":return <Imports notify={notify}/>;case"avo":return <AVOChat notify={notify}/>;case"recommendations":return <Recommendations notify={notify}/>;case"actions":return <Actions notify={notify} role={role}/>;case"marketing":return <Marketing go={go}/>;case"campaign-studio":return <CampaignStudio notify={notify} role={role}/>;case"campaign-calendar":return <Calendar/>;case"analytics":return <Analytics/>;case"governance":return <Governance notify={notify}/>;case"audit":return <Audit notify={notify}/>;case"settings":return <SettingsPage notify={notify}/>;default:return null}
}

function Overview({go}:{go:(p:string)=>void}){
 const atRisk=customers.filter(c=>c.risk==="High"||c.risk==="Critical");
 return <><div className="grid stats">{[["Customers monitored","30","All synthetic records",Users],["High / Critical risk",`${atRisk.length}`,"2 require action today",AlertTriangle],["Revenue at risk",money(atRisk.reduce((s,c)=>s+c.revenueAtRisk,0)),"↓ 6.8% after recoveries",Gauge],["Pending approvals","4","2 retention · 2 marketing",ClipboardCheck]].map(([l,v,t,I])=><div className="card" key={String(l)}><div className="split"><span className="subtle">{String(l)}</span>{typeof I!=="string"&&<I size={18} color="#19766e"/>}</div><div className="stat-value">{String(v)}</div><div className="trend">{String(t)}</div></div>)}</div>
 <div className="grid two"><div className="card"><div className="card-head"><h2>Risk and revenue trend</h2><button className="btn btn-outline" onClick={()=>go("analytics")}>View analytics <ChevronRight size={13}/></button></div><ResponsiveContainer width="100%" height={250}><AreaChart data={trendChart}><defs><linearGradient id="risk" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#dd684d" stopOpacity={.35}/><stop offset="1" stopColor="#dd684d" stopOpacity={.02}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" stroke="#edf0ed"/><XAxis dataKey="month" tick={{fontSize:10}}/><YAxis tick={{fontSize:10}}/><Tooltip/><Area type="monotone" dataKey="risk" stroke="#dd684d" fill="url(#risk)" strokeWidth={2}/></AreaChart></ResponsiveContainer></div>
 <div className="card"><div className="card-head"><h2>Today’s priorities</h2><span className="badge high">4 open</span></div><Priority customer={customers[0]} text="Critical risk · service recovery due"/><Priority customer={customers[2]} text="Segment price objection pattern"/><Priority customer={customers[1]} text="Grounded cross-sell opportunity"/><div className="notice warning" style={{marginTop:10}}>AVO supports staff decisions. Final decisions and actions remain the responsibility of authorised employees.</div></div></div>
 <div className="grid two" style={{marginTop:16}}><div className="card"><div className="card-head"><h2>Customers needing attention</h2><button className="btn btn-outline" onClick={()=>go("customers")}>All customers</button></div><CustomerTable rows={customers.filter(c=>c.alerts).slice(0,5)}/></div><div className="card"><div className="card-head"><h2>AVO governance pulse</h2><span className="demo-label">Synthetic metrics</span></div><div className="kpis"><div className="kpi"><strong>82%</strong><span>approved after review</span></div><div className="kpi"><strong>31%</strong><span>human-edited</span></div><div className="kpi"><strong>4</strong><span>AVO abstentions</span></div></div><div className="divider"/><div className="subtle">Every material output includes evidence, confidence and an approval requirement.</div></div></div></>
}
function Priority({customer,text}:{customer:Customer;text:string}){return <div className="split" style={{padding:"10px 0",borderBottom:"1px solid #eef0ed"}}><div className="customer"><div className="customer-dot">{customer.name.split(" ").map(x=>x[0]).join("")}</div><div><strong style={{fontSize:12}}>{customer.name}</strong><div className="subtle" style={{fontSize:10}}>{text}</div></div></div>{badge(customer.risk)}</div>}
function CustomerTable({rows,onSelect}:{rows:Customer[];onSelect?:(c:Customer)=>void}){return <div className="table-wrap"><table className="table"><thead><tr><th>Customer</th><th>Tier</th><th>Risk</th><th>Score</th><th>Revenue at risk</th><th>Owner</th></tr></thead><tbody>{rows.map(c=><tr key={c.id} onClick={()=>onSelect?.(c)} style={{cursor:onSelect?"pointer":"default"}}><td><div className="customer"><div className="customer-dot">{c.name.slice(0,2).toUpperCase()}</div><div><strong>{c.name}</strong><div className="subtle" style={{fontSize:9}}>{c.company}</div></div></div></td><td>{badge(c.tier)}</td><td>{badge(c.risk)}</td><td><strong>{c.riskScore}</strong></td><td>{money(c.revenueAtRisk)}</td><td>{c.staff}</td></tr>)}</tbody></table></div>}

function Alerts({go,notify}:{go:(p:string)=>void;notify:(s:string)=>void}){const rows=customers.filter(c=>c.alerts);return <div className="card"><div className="card-head"><div><h2>Open alerts</h2><div className="subtle">Generated from deterministic thresholds and traceable evidence.</div></div><button className="btn btn-outline" onClick={()=>notify("Alert filters applied")}>Filter alerts</button></div><div className="table-wrap"><table className="table"><thead><tr><th>Customer</th><th>Trigger</th><th>Risk</th><th>Confidence</th><th>Deadline</th><th>Status</th><th></th></tr></thead><tbody>{rows.map((c,i)=><tr key={c.id}><td><strong>{c.name}</strong><div className="subtle">{c.id} · {c.tier}</div></td><td>{c.scenario==="A"?"Cancellation + missed follow-up":"Elevated risk threshold"}</td><td>{badge(c.risk)} <strong>{c.riskScore}</strong></td><td>{c.confidence}%</td><td>{c.risk==="Critical"?"Within 24h":"Within 48h"}</td><td>{badge(i?"Assigned":"New")}</td><td><button className="btn btn-outline" onClick={()=>go("conversations")}>Review evidence</button></td></tr>)}</tbody></table></div></div>}

function Customers({notify}:{notify:(s:string)=>void}){const [selected,setSelected]=useState<Customer|null>(null);const [query,setQuery]=useState("");const filtered=customers.filter(c=>(c.name+c.company).toLowerCase().includes(query.toLowerCase()));if(selected)return <Customer360 customer={selected} back={()=>setSelected(null)} notify={notify}/>;return <div className="card"><div className="card-head"><div className="field" style={{width:320}}><label>Search customer or company</label><div style={{position:"relative"}}><Search size={14} style={{position:"absolute",left:10,top:11}}/><input className="input" style={{paddingLeft:32}} value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search 30 synthetic customers"/></div></div><button className="btn btn-outline" onClick={()=>notify("Customer list exported as CSV")}>Export view <Download size={14}/></button></div><CustomerTable rows={filtered} onSelect={setSelected}/></div>}

function Customer360({customer:c,back,notify}:{customer:Customer;back:()=>void;notify:(s:string)=>void}){const [tab,setTab]=useState("Overview");return <><button className="btn btn-outline" onClick={back}>← Back to customers</button><div className="card" style={{marginTop:12}}><div className="split"><div className="customer"><div className="customer-dot" style={{width:48,height:48,fontSize:16}}>{c.name.slice(0,2)}</div><div><h2 style={{margin:0}}>{c.name}</h2><div className="subtle">{c.company} · {c.id} · Assigned to {c.staff}</div></div></div><div className="top-actions">{badge(c.tier)}{badge(c.risk)}<button className="btn btn-primary" onClick={()=>notify("AVO analysis opened for staff review")}><Bot size={14}/> Run AVO Analysis</button></div></div><div className="divider"/><div className="kpis"><div className="kpi"><strong>{c.riskScore}/100</strong><span>hybrid churn risk</span></div><div className="kpi"><strong>{c.confidence}%</strong><span>calculation confidence</span></div><div className="kpi"><strong>{money(c.ltv)}</strong><span>lifetime value</span></div><div className="kpi"><strong>{money(c.revenueAtRisk)}</strong><span>revenue at risk</span></div><div className="kpi"><strong>{c.frequencyTrend}%</strong><span>purchase frequency trend</span></div></div></div>
 <div className="card" style={{marginTop:14}}><div className="tabs">{["Overview","Transactions","Conversations","AVO Insights","Alerts","Actions","Campaign History","Audit History"].map(t=><button key={t} onClick={()=>setTab(t)} className={`tab ${tab===t?"active":""}`}>{t}</button>)}</div>{tab==="Overview"?<div className="grid two"><div><h3>Customer health</h3><div className="evidence"><div className="split"><span>Tier score</span><strong>{c.tierScore}/100</strong></div><div className="bar"><span style={{width:`${c.tierScore}%`,background:"#19766e"}}/></div><div className="subtle" style={{marginTop:6}}>Deterministic: recency, frequency, monetary value, lifetime value, product diversity and relationship duration · tier-v1.0</div></div><div className="evidence"><strong>Product history</strong><div className="subtle">{c.products.join(" · ")}</div>{c.productGap&&<div style={{marginTop:6}}>Product gap: <strong>{c.productGap}</strong></div>}</div><div className="evidence"><strong>Consent and channel</strong><div className="subtle">Marketing consent: {c.consent?"Granted":"Not granted"} · Preferred: {c.preferredChannel}</div></div></div><div><h3>Top risk factors</h3>{[["Purchase frequency deterioration",Math.abs(Math.min(c.frequencyTrend,0))*1.3],["Spending deterioration",Math.abs(Math.min(c.spendTrend,0))*1.2],["Conversation signals",c.sentiment==="Negative"?20:4],["Unresolved complaints",c.scenario==="A"?24:0]].map(([n,v])=><div key={String(n)} style={{margin:"12px 0"}}><div className="split"><span className="subtle">{n}</span><strong>{Math.round(Number(v))} pts</strong></div><div className="bar"><span style={{width:`${Math.min(100,Number(v)*3)}%`,background:"#dd684d"}}/></div></div>)}</div></div>:tab==="Conversations"?<div>{c.messages.map(m=><div className="evidence" key={m.id}><span className="evidence-id">{m.id} · {m.channel}</span><div>{m.text}</div></div>)}</div>:<div className="empty"><FileText size={30}/><p>{tab} records are included in the synthetic demo lineage.</p></div>}</div></>}

function Conversations({notify}:{notify:(s:string)=>void}){const eligible=customers.filter(c=>c.messages.length);const [selected,setSelected]=useState(eligible[0]);const [analysis,setAnalysis]=useState<AVOAnalysis|null>(null);const [loading,setLoading]=useState(false);const run=async()=>{setLoading(true);const r=await fetch("/api/avo/analyze",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({customerId:selected.id})});const data=await r.json();setAnalysis(data.analysis);setLoading(false);notify(data.demo?"AVO Demo Analysis completed — fallback clearly labelled":"AVO analysis completed with configured model")};return <div className="conversation-grid"><div className="card" style={{padding:0}}><div style={{padding:12}}><input className="input" placeholder="Search conversations"/></div><div className="conversation-list">{eligible.slice(0,10).map(c=><div key={c.id} className={`conversation-item ${selected.id===c.id?"active":""}`} onClick={()=>{setSelected(c);setAnalysis(null)}}><div className="split"><strong style={{fontSize:12}}>{c.name}</strong>{badge(c.risk)}</div><div className="subtle" style={{fontSize:10,margin:"5px 0"}}>{c.messages.at(-1)?.text.slice(0,65)}…</div><div className="split">{badge(c.sentiment)}<span className="subtle" style={{fontSize:9}}>{c.messages.at(-1)?.channel}</span></div></div>)}</div></div>
 <div className="card"><div className="card-head"><div><h2>{selected.name}</h2><div className="subtle">{selected.company} · {selected.preferredChannel}</div></div><button className="btn btn-primary" onClick={run} disabled={loading}><Bot size={14}/>{loading?"Analysing…":"Run AVO Analysis"}</button></div><div className="messages">{selected.messages.map(m=><div key={m.id} className={`message ${m.sender==="customer"?"customer-msg":"staff-msg"}`}><div>{m.evidence?<mark className="source-highlight">{m.text}</mark>:m.text}</div><div className="message-meta">{m.id} · {m.senderName} · {m.channel} · {new Date(m.sentAt).toLocaleDateString()}</div></div>)}</div></div>
 <div className="card"><div className="card-head"><h2>AVO Analysis</h2>{analysis&&<span className="demo-label">AVO Demo Analysis</span>}</div>{!analysis?<div className="empty"><Bot size={34}/><p>Run AVO to extract grounded signals. Customer messages are treated as untrusted content.</p></div>:<AnalysisPanel analysis={analysis} notify={notify}/>}</div></div>}
function AnalysisPanel({analysis:a,notify}:{analysis:AVOAnalysis;notify:(s:string)=>void}){return <div><div className="notice warning">AVO-generated analysis. Verify evidence before approval.</div><p style={{fontSize:12,lineHeight:1.55}}>{a.concise_summary}</p><div className="split">{badge(a.sentiment_label)}{badge(a.urgency)}<strong>{Math.round(a.analysis_confidence*100)}% confidence</strong></div><div className="divider"/><h3>Signals</h3>{[["Intent",[a.primary_intent]],["Complaints",a.complaints],["Unresolved",a.unresolved_issues],["Cancellation",a.cancellation_signals],["Missed follow-ups",a.missed_follow_ups]].map(([k,v])=><div className="split" style={{margin:"8px 0"}} key={String(k)}><span className="subtle">{k}</span><strong style={{fontSize:11,textAlign:"right"}}>{(v as string[]).join(", ")||"None found"}</strong></div>)}<div className="divider"/><h3>Source evidence</h3>{a.evidence.map(e=><div className="evidence" key={e.message_id}><span className="evidence-id">{e.message_id} · {e.evidence_type}</span><div>{e.short_explanation}</div></div>)}<div className="notice" style={{marginTop:10}}>{a.uncertainty_reason}</div><button className="btn btn-primary" style={{width:"100%",marginTop:12}} onClick={()=>notify("Draft AVO recommendation created; manager approval required")}>Generate AVO Recommendation</button></div>}

function Imports({notify}:{notify:(s:string)=>void}){const [step,setStep]=useState(0);const [file,setFile]=useState("");const templates=["customers.csv","transactions.csv","conversations.csv","conversations.json","products.csv","campaign-results.csv","retention-playbook.pdf","customer-service-policy.pdf","product-catalogue.pdf","marketing-guidelines.pdf","existing-campaign.png"];const upload=(e:React.ChangeEvent<HTMLInputElement>)=>{const f=e.target.files?.[0];if(!f)return;setFile(f.name);setStep(1);notify("File type and size scan passed")};return <div className="grid two"><div className="card"><div className="card-head"><h2>New import</h2><span className="badge medium">Administrator</span></div><div className="tabs">{["1 Upload","2 Preview & map","3 Validate","4 Confirm"].map((x,i)=><span className={`tab ${step===i?"active":""}`} key={x}>{x}</span>)}</div>{step===0?<label className="upload-zone" style={{display:"block",cursor:"pointer"}}><Upload size={30} color="#19766e"/><h3 style={{marginTop:10}}>Upload an authorised data file</h3><p className="subtle">CSV, XLSX, JSON, TXT, PDF, DOCX, PNG or JPG · max 10 MB</p><input type="file" hidden onChange={upload}/><span className="btn btn-primary">Choose file</span></label>:step===1?<div><div className="notice"><strong>{file}</strong> passed type and malware-safe extension checks.</div><div className="divider"/><h3>Column mapping preview</h3>{["customer_external_id","customer_name","company_name","assigned_staff_email","consent_status"].map(x=><div className="split evidence" key={x}><span>{x}</span><select className="input" style={{width:190}}><option>{x}</option><option>Ignore column</option></select></div>)}<button className="btn btn-primary" onClick={()=>setStep(2)}>Validate 30 preview rows</button></div>:step===2?<div><div className="notice">30 valid rows · 0 duplicates · 0 invalid required fields</div><div className="grid three" style={{margin:"18px 0"}}><div className="card"><strong>30</strong><div className="subtle">Ready</div></div><div className="card"><strong>0</strong><div className="subtle">Duplicates</div></div><div className="card"><strong>0</strong><div className="subtle">Invalid</div></div></div><button className="btn btn-primary" onClick={()=>setStep(3)}>Continue to confirmation</button> <button className="btn btn-outline" onClick={()=>notify("Empty error report downloaded")}>Error report</button></div>:<div><div className="notice warning">Confirmation records your identity, selected mapping and validation result in the immutable audit log.</div><p><strong>Uploader:</strong> Farah Chen · Administrator</p><p><strong>File:</strong> {file}</p><button className="btn btn-primary" onClick={()=>{notify("Import confirmed and audited — 30 records created");setStep(0);setFile("")}}>Confirm import</button></div>}</div>
 <div className="card"><div className="card-head"><div><h2>Demo Data and Templates</h2><div className="subtle">Permanent synthetic files for manual import.</div></div><span className="demo-label">Synthetic Demo Data</span></div>{templates.map(f=><div className="split" style={{padding:"9px 0",borderBottom:"1px solid #eef0ed"}} key={f}><div><strong style={{fontSize:12}}>{f}</strong><div className="subtle" style={{fontSize:9}}>{f.endsWith(".pdf")?"Approved demo source document":"Populated mock template"}</div></div><a className="btn btn-outline" href={`/api/demo-files/${f}`} download><Download size={13}/> Download</a></div>)}<div className="divider"/><div className="top-actions"><button className="btn btn-outline" onClick={()=>notify("Blank templates bundle prepared")}>Blank templates</button><button className="btn btn-danger" onClick={()=>notify("Demo reset requires a second confirmation in Settings")}>Reset demo</button></div></div></div>}

function AVOChat({notify}:{notify:(s:string)=>void}){const suggestions=["Which Strategic customers are at risk?","Why is Maya Tan Critical Risk?","Which segment has the greatest decline?","What actions await my approval?"];const [chat,setChat]=useState<{from:string;text:string}[]>([{from:"avo",text:"Hello, I’m AVO. I can explain customer evidence and create drafts, but I cannot approve or execute actions."}]);const [input,setInput]=useState("");const send=(q=input)=>{if(!q.trim())return;const answer=q.includes("Maya")?"Maya Tan (CUS-1001) has a deterministic risk score of 86. The strongest sources are MSG-A-101 (unresolved delivery), MSG-A-103 (missed follow-up and competitor mention), and MSG-A-104 (cancellation language). This suggests elevated risk; it does not confirm future churn.":q.includes("segment")?"Food & beverage / North shows a 24% frequency decline, 18% revenue decline and repeated price objections across 33% of the segment. Review Marketing Trigger MKT-003.":q.includes("approval")?"Four items await review: two retention actions and two campaigns. I can open or summarize them, but only an authorised manager can decide.":"I found relevant synthetic records, but there is insufficient evidence for a firm conclusion—staff review required.";setChat(x=>[...x,{from:"user",text:q},{from:"avo",text:answer}]);setInput("");notify("AVO answer grounded to authorised demo records")};return <div className="grid two"><div className="card"><div className="card-head"><h2>AVO Chat</h2><span className="demo-label">Organisation-scoped</span></div><div style={{minHeight:360,maxHeight:480,overflow:"auto"}}>{chat.map((m,i)=><div className={`message ${m.from==="user"?"staff-msg":"customer-msg"}`} key={i}><strong style={{fontSize:10}}>{m.from==="avo"?"AVO Demo Analysis":"You"}</strong><div>{m.text}</div>{m.from==="avo"&&i>0&&<div className="message-meta">Evidence links included · uncertainty stated</div>}</div>)}</div><div className="top-actions"><input className="input" value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Ask AVO about accessible customers, evidence or approvals"/><button className="btn btn-primary" onClick={()=>send()}><Send size={14}/></button></div></div><div className="card"><h2>Suggested questions</h2><div className="divider"/>{suggestions.map(s=><button key={s} className="btn btn-outline" style={{display:"flex",width:"100%",justifyContent:"space-between",marginBottom:8}} onClick={()=>send(s)}>{s}<ChevronRight size={13}/></button>)}<div className="notice warning" style={{marginTop:18}}>AVO can create draft recommendations only. It cannot approve, change scores, send private messages or publish content.</div><div className="notice" style={{marginTop:10}}>Data minimisation is active: unnecessary contact details are masked before external AI processing.</div></div></div>}

function Recommendations({notify}:{notify:(s:string)=>void}){const [expanded,setExpanded]=useState("REC-001");return <div className="grid two"><div className="card"><div className="card-head"><h2>Recommendation queue</h2><span className="badge medium">Human review required</span></div>{recommendations.map(r=>{const c=customers.find(x=>x.id===r.customerId)!;return <div key={r.id} className="evidence" onClick={()=>setExpanded(r.id)} style={{cursor:"pointer",borderColor:expanded===r.id?"#69a99e":undefined}}><div className="split"><span className="evidence-id">{r.id} · {c.name}</span>{badge(r.status)}</div><strong>{r.action}</strong><div className="subtle">{r.priority} · {r.channel} · {r.confidence} confidence</div></div>})}</div><RecommendationDetail rec={recommendations.find(r=>r.id===expanded)!} notify={notify}/></div>}
function RecommendationDetail({rec:r,notify}:{rec:(typeof recommendations)[number];notify:(s:string)=>void}){const c=customers.find(x=>x.id===r.customerId)!;return <div className="card"><div className="card-head"><h2>AVO Recommendation</h2>{badge(r.confidence)}</div><div className="notice warning">AVO-generated recommendation. Verify evidence before approval.</div><h3 style={{marginTop:16}}>{r.action}</h3><p className="subtle" style={{lineHeight:1.6}}>{r.explanation}</p><div className="grid two"><div className="field"><label>Owner</label><input className="input" defaultValue={r.owner}/></div><div className="field"><label>Deadline</label><input className="input" defaultValue={r.deadline}/></div></div><div className="field" style={{marginTop:12}}><label>Editable customer draft</label><textarea className="input" rows={5} defaultValue={c.scenario==="A"?"Hi Maya, I’m sorry our promised update was missed. I’m reviewing the replacement status under our service policy and will confirm the next step after manager approval.":"Hi Ethan, based on your question about campaign performance, I can share the approved Analytics Suite overview if helpful."}/></div><h3 style={{marginTop:16}}>Evidence and policy sources</h3>{c.messages.filter(m=>m.evidence).slice(0,3).map(m=><div className="evidence" key={m.id}><span className="evidence-id">{m.id} · Source evidence</span><div>{m.text}</div></div>)}<div className="evidence"><span className="evidence-id">customer-service-policy.pdf · page 2</span><div>Service recovery must precede promotional outreach for unresolved complaints.</div></div><div className="notice">Uncertainty: the customer’s future behaviour cannot be confirmed. Staff must validate current delivery status.</div><div className="top-actions" style={{marginTop:13}}><button className="btn btn-primary" onClick={()=>notify("Original AVO output and edited draft saved; submitted for manager approval")}>Submit for approval</button><button className="btn btn-outline" onClick={()=>notify("Recommendation marked Useful")}>Useful</button><button className="btn btn-outline" onClick={()=>notify("Correction note required when marking Incorrect")}>Incorrect</button></div></div>}

function Actions({notify,role}:{notify:(s:string)=>void;role:Role}){const [status,setStatus]=useState("Pending Approval");const c=customers[0];const manager=role==="Sales Manager"||role==="Administrator";const approved=status==="Approved"||status==="Executed";const message="Hi Maya, I’m sorry our promised update was missed. I’m reviewing the replacement under our approved service policy and will confirm the next step.";return <div className="grid two"><div className="card"><div className="card-head"><h2>ACT-021 · Service recovery</h2>{badge(status)}</div><div className="customer"><div className="customer-dot">MT</div><div><strong>{c.name}</strong><div className="subtle">{c.company} · consent verified</div></div></div><div className="divider"/><div className="timeline">{[["Draft created","Aisha Rahman · 09:38"],["Submitted for approval","Aisha Rahman · 09:42"],[status==="Rejected"?"Rejected":approved?"Approved":"Awaiting Sales Manager",approved?"Farah Chen · 10:05":"No decision yet"],[status==="Executed"?"WhatsApp link opened":"Execution pending",status==="Executed"?"Aisha Rahman · 10:11":"Requires approval"]].map(([a,b],i)=><div className="timeline-item" key={String(a)}><span className="timeline-dot" style={{borderColor:i<2||approved?"#19766e":"#c8ceca"}}/><strong>{a}</strong><div className="subtle">{b}</div></div>)}</div></div><div className="card"><div className="card-head"><h2>Approval review</h2><span className="badge strategic">Sales Manager</span></div><div className="notice warning">AVO cannot approve its own recommendation. Original and human-edited versions are retained.</div><div className="field" style={{marginTop:14}}><label>Approved message draft</label><textarea className="input" rows={5} defaultValue={message}/></div><div className="field" style={{marginTop:10}}><label>Reviewer comment</label><input className="input" placeholder="Record the basis for the decision"/></div>{!approved?<div className="top-actions" style={{marginTop:12}}><button className="btn btn-primary" disabled={!manager} onClick={()=>{setStatus("Approved");notify("Action approved and audit event recorded")}}>Approve</button><button className="btn btn-danger" disabled={!manager} onClick={()=>{setStatus("Rejected");notify("Action rejected; reason is required in production")}}>Reject</button><button className="btn btn-outline" disabled={!manager} onClick={()=>notify("Changes requested and requester notified")}>Request changes</button></div>:<div className="top-actions" style={{marginTop:12}}><a className="btn btn-primary" href={canOutreach(c,"WhatsApp")?whatsappLink(c.phone,message):undefined} target="_blank" rel="noreferrer" onClick={()=>{setStatus("Executed");notify("Approved WhatsApp deep link opened; no message was auto-sent")}}><MessageCircle size={14}/> Open approved WhatsApp</a><a className="btn btn-outline" href={`mailto:${c.email}?subject=${encodeURIComponent("Service recovery follow-up")}&body=${encodeURIComponent(message)}`}><Mail size={14}/> Compose email</a></div>}{!manager&&<p className="subtle">Switch to Sales Manager or Administrator demo role to approve.</p>}</div></div>}

function Marketing({go}:{go:(p:string)=>void}){const detection=detectSegmentDecline(4,12,18,24,29);return <><div className="grid stats">{[["Active triggers","3"],["Customers affected","12"],["Revenue decline","18%"],["Shared themes","3"]].map(([a,b])=><div className="card" key={a}><div className="subtle">{a}</div><div className="stat-value">{b}</div><span className="demo-label">Synthetic metric</span></div>)}</div><div className="card"><div className="card-head"><div><h2>MKT-003 · North food & beverage decline</h2><div className="subtle">System-detected · 18 July 2026</div></div><span className="badge high">Intervention recommended</span></div><div className="grid three"><div className="evidence"><span className="evidence-id">DETERMINISTIC CALCULATION</span><strong style={{display:"block",fontSize:20}}>{detection.affectedPercentage}%</strong><span className="subtle">4 of 12 at High/Critical risk · threshold 20%</span></div><div className="evidence"><span className="evidence-id">TRANSACTION EVIDENCE</span><strong style={{display:"block",fontSize:20}}>-18%</strong><span className="subtle">Segment revenue vs prior period · threshold -15%</span></div><div className="evidence"><span className="evidence-id">ENGAGEMENT EVIDENCE</span><strong style={{display:"block",fontSize:20}}>-29%</strong><span className="subtle">Reply and campaign engagement · threshold -25%</span></div></div><div className="grid two" style={{marginTop:14}}><div><h3>Common evidence and themes</h3>{["Package price difficult to justify — 5 customers","Purchase frequency down 24%","Lower engagement across email campaigns"].map((x,i)=><div className="evidence" key={x}><span className="evidence-id">{i?"Transaction aggregate":"Conversation sources · 5 valid message IDs"}</span><div>{x}</div></div>)}</div><div><h3>AVO campaign recommendation</h3><div className="notice">Create a value-education campaign grounded in the approved product catalogue and marketing guidelines. Do not invent a discount or imply guaranteed savings.</div><p className="subtle">Confidence: Medium · Pricing sensitivity is observed, but causal attribution remains uncertain.</p><button className="btn btn-primary" onClick={()=>go("campaign-studio")}><Sparkles size={14}/> Create campaign with AVO</button></div></div></div></>}

function CampaignStudio({notify,role}:{notify:(s:string)=>void;role:Role}){const [stage,setStage]=useState(1);const [approved,setApproved]=useState(false);const [scheduled,setScheduled]=useState(false);const manager=role==="Marketing Manager"||role==="Administrator";const schedule=async()=>{const r=await fetch("/api/publish",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({campaignId:"CAM-003",channelId:"demo-linkedin",text:"Make every order work harder with clearer inventory insight. Explore the approved Inventory Optimizer guide.",dueAt:"2026-07-24T02:00:00.000Z",approved,idempotencyKey:"CAM-003-demo-linkedin-20260724"})});const d=await r.json();if(d.error)return notify(d.error);setScheduled(true);notify(d.simulated?"Scheduled with Demo Publisher — simulated, not sent":"Campaign scheduled through Buffer")};return <div className="grid two"><div className="card"><div className="card-head"><h2>CAM-003 · Value clarity</h2>{badge(scheduled?"Scheduled":approved?"Approved":"Draft")}</div><div className="tabs">{["1 Brief","2 Content","3 Review","4 Approval","5 Schedule"].map((x,i)=><button key={x} className={`tab ${stage===i+1?"active":""}`} onClick={()=>setStage(i+1)}>{x}</button>)}</div><div className="field"><label>Objective</label><input className="input" defaultValue="Re-engage North food & beverage customers with approved product value education"/></div><div className="field" style={{marginTop:10}}><label>Consented audience</label><input className="input" defaultValue="8 of 12 customers · 4 excluded by consent guardrail" readOnly/></div><h3 style={{marginTop:16}}>Approved source materials</h3>{["product-catalogue.pdf · pages 2–4","marketing-guidelines.pdf · pages 1–3","MKT-003 aggregate evidence"].map(x=><label className="evidence" key={x} style={{display:"block"}}><input type="checkbox" defaultChecked/> {x}</label>)}<button className="btn btn-primary" onClick={()=>{setStage(2);notify("AVO Demo Analysis generated grounded campaign variants")}}><Bot size={14}/> Generate with AVO</button></div>
 <div className="card"><div className="card-head"><h2>Channel content</h2><span className="demo-label">AVO Demo Analysis</span></div><div className="notice warning">Factual claim review: no discount, price, inventory or outcome claims were introduced.</div><div className="field" style={{marginTop:12}}><label>LinkedIn caption</label><textarea className="input" rows={4} defaultValue="Make every order work harder with clearer inventory insight. Explore how the approved Inventory Optimizer can support planning—without changing how your team works overnight."/></div><div className="field" style={{marginTop:10}}><label>Email subject</label><input className="input" defaultValue="A clearer view of your inventory planning"/></div><div className="field" style={{marginTop:10}}><label>Email / WhatsApp campaign message</label><textarea className="input" rows={4} defaultValue="See the approved Inventory Optimizer overview and decide whether it fits your current planning needs. Reply if you would like a staff-led walkthrough."/></div><div className="evidence"><span className="evidence-id">CLAIM SOURCE</span><div>product-catalogue.pdf · page 3 · Inventory Optimizer description</div></div>{!approved?<div className="top-actions"><button className="btn btn-primary" onClick={()=>notify("Campaign submitted; original AVO output and current version preserved")}>Submit for approval</button><button className="btn btn-primary" disabled={!manager} onClick={()=>{setApproved(true);setStage(5);notify("Marketing Manager approved campaign")}}>Approve as manager</button></div>:<button className="btn btn-primary" onClick={schedule} disabled={scheduled}><CalendarDays size={14}/>{scheduled?"Scheduled (simulated)":"Schedule with Demo Publisher"}</button>}{!manager&&!approved&&<p className="subtle">Switch to Marketing Manager to approve.</p>}</div></div>}

function Calendar(){return <div className="grid three">{[["24 Jul","Value clarity","LinkedIn · 10:00","Scheduled · Demo Publisher"],["26 Jul","Product planning guide","Instagram · 12:30","Approved"],["30 Jul","Recovery stories","Facebook · 09:00","Draft"]].map(([d,n,t,s])=><div className="card" key={n}><div className="eyebrow">{d} · 2026</div><h2 style={{margin:"9px 0"}}>{n}</h2><div className="subtle">{t}</div><div className="divider"/>{badge(s)}</div>)}</div>}

function Analytics(){return <><div className="grid stats">{[["Recovered customers","6","+2 this quarter"],["Estimated recovered revenue",money(128400),"Synthetic estimate"],["Actions completed","18","78% approval rate"],["Campaign conversions","7","Demo outcomes"]].map(([a,b,c])=><div className="card" key={a}><div className="subtle">{a}</div><div className="stat-value">{b}</div><div className="trend">{c}</div></div>)}</div><div className="grid two"><div className="card"><div className="card-head"><h2>Customers by tier</h2><span className="demo-label">Synthetic</span></div><ResponsiveContainer width="100%" height={240}><BarChart data={tiersChart}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name" tick={{fontSize:10}}/><YAxis tick={{fontSize:10}}/><Tooltip/><Bar dataKey="value" fill="#19766e" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div><div className="card"><div className="card-head"><h2>Customers by risk</h2><span className="demo-label">Synthetic</span></div><ResponsiveContainer width="100%" height={240}><PieChart><Pie data={riskChart} dataKey="value" nameKey="name" innerRadius={55} outerRadius={85} paddingAngle={3}>{["#58a884","#dec453","#e89b45","#dd684d"].map(x=><Cell key={x} fill={x}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></div><div className="grid two" style={{marginTop:14}}><div className="card"><h2>Successful recovery · Omar Aziz</h2><div className="divider"/><div className="timeline"><div className="timeline-item"><span className="timeline-dot"/><strong>High risk · 68</strong><div className="subtle">Unresolved replacement issue</div></div><div className="timeline-item"><span className="timeline-dot"/><strong>Approved service recovery</strong><div className="subtle">Human-reviewed WhatsApp action</div></div><div className="timeline-item"><span className="timeline-dot"/><strong>Positive response and new purchase</strong><div className="subtle">Risk recalculated to Medium · 42</div></div></div><div className="notice">Estimated recovered revenue: {money(22100)} · based on observed subsequent purchase, labelled estimate.</div></div><div className="card"><h2>AVO governance</h2><div className="divider"/>{[["Generated",42],["Approved unchanged",19],["Edited before approval",13],["Rejected",6],["Marked incorrect",1],["Low confidence / abstained",4]].map(([a,b])=><div className="split evidence" key={String(a)}><span>{a}</span><strong>{b}</strong></div>)}</div></div></>}

function Governance({notify}:{notify:(s:string)=>void}){const controls=[["Purpose limitation","Retention, service and consented marketing purposes only.",CheckCircle2],["Data minimisation","Contact details and unrelated identifiers masked before AVO processing.",ShieldCheck],["Organisation isolation","Supabase RLS policies scope records by organisation and role.",Users],["AI processing disclosure","Demo provider active; external model data categories shown before use.",Bot],["Retention controls","Expiry flags require administrator confirmation; no automatic deletion.",Archive],["Sensitive exports","Approval and audit event required before an export is prepared.",Download]];return <><div className="notice">AVO supports staff decisions. Final decisions and actions remain the responsibility of authorised employees.</div><div className="grid three" style={{marginTop:14}}>{controls.map(([a,b,I])=><div className="card" key={String(a)}>{typeof I!=="string"&&<I size={20} color="#19766e"/>}<h3 style={{marginTop:10}}>{String(a)}</h3><p className="subtle" style={{lineHeight:1.5}}>{String(b)}</p><span className="badge low">Control active</span></div>)}</div><div className="grid two" style={{marginTop:14}}><div className="card"><h2>Customer rights workflow</h2><div className="divider"/>{["View and correct customer data","Withdraw marketing consent","Request approved data export","Mark data for deletion review"].map(x=><div className="split evidence" key={x}><span>{x}</span><button className="btn btn-outline" onClick={()=>notify(`${x} request recorded for review`)}>Start request</button></div>)}</div><div className="card"><h2>Data lineage · CUS-1001</h2><div className="divider"/><div className="timeline">{["customers.csv · original upload","IMP-001 · confirmed import","CUS-1001 · customer record","ANL-021 · AVO analysis","ALT-001 · Critical alert","REC-001 · recommendation","APR-021 · pending approval"].map(x=><div className="timeline-item" key={x}><span className="timeline-dot"/><strong>{x}</strong></div>)}</div></div></div></>}

function Audit({notify}:{notify:(s:string)=>void}){const [query,setQuery]=useState("");const rows=audits.filter(a=>JSON.stringify(a).toLowerCase().includes(query.toLowerCase()));return <div className="card"><div className="card-head"><div className="field" style={{width:320}}><label>Search immutable events</label><input className="input" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Action, actor, entity or correlation ID"/></div><div className="top-actions"><button className="btn btn-outline" onClick={()=>notify("Audit CSV export prepared and export event logged")}><Download size={13}/> CSV</button><button className="btn btn-outline" onClick={()=>window.print()}><FileText size={13}/> Printable report</button></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Event</th><th>Time</th><th>Actor / role</th><th>Action</th><th>Entity</th><th>Result</th><th>Correlation</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td><strong>{a.id}</strong></td><td>{a.at}</td><td>{a.actor}<div className="subtle">{a.role}</div></td><td>{a.action}</td><td>{a.entity}</td><td>{badge(a.result)}</td><td>{a.correlationId}</td></tr>)}</tbody></table></div><div className="notice" style={{marginTop:14}}>Normal users cannot modify or delete audit records. Export actions create their own audit events.</div></div>}

function SettingsPage({notify}:{notify:(s:string)=>void}){return <div className="grid two"><div className="card"><h2>Scoring and trigger thresholds</h2><div className="divider"/>{[["High risk starts at","60"],["Critical risk starts at","80"],["Segment customers at risk (%)","20"],["Segment revenue decline (%)","15"],["Purchase frequency decline (%)","20"],["Engagement decline (%)","25"]].map(([a,b])=><div className="field" style={{marginBottom:10}} key={a}><label>{a}</label><input className="input" type="number" defaultValue={b}/></div>)}<button className="btn btn-primary" onClick={()=>notify("Governance setting change saved and audited")}>Save thresholds</button></div><div><div className="card"><div className="card-head"><h2>Integration status</h2><span className="badge medium">Demo mode</span></div><div className="split evidence"><span>AVO provider</span><strong>DemoAVOProvider</strong></div><div className="split evidence"><span>Social publisher</span><strong>DemoSocialPublisher</strong></div><div className="notice">Add server-side credentials to activate OpenAI GPT-5.6 and Buffer. Secrets are never exposed to the browser.</div></div><div className="card" style={{marginTop:14}}><h2>Demo accounts</h2><div className="divider"/>{demoAccounts.map(([email,role])=><div className="split evidence" key={email}><span>{email}</span><strong>{role}</strong></div>)}<div className="subtle">Password: <strong>PulseDemo!2026</strong> · synthetic local demo only</div></div><div className="card" style={{marginTop:14}}><h2>Demo controls</h2><p className="subtle">Reset and reload the seeded synthetic organisation. This action requires explicit confirmation in a connected Supabase environment.</p><button className="btn btn-danger" onClick={()=>notify("Second confirmation required before resetting demo data")}>Reset seeded demo</button></div></div></div>}
